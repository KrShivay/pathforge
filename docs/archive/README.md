# Archived imports

This directory preserves the original pathology context bundle and its
flattened import for provenance. Do not edit or link to these copies for active
project work; use the structured documentation under the parent `docs/`
directory instead.
